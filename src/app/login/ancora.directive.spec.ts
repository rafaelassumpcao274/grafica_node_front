import { AncoraDirective } from './AdDirective.directive';

describe('AncoraDirective', () => {
  it('should create an instance', () => {
    const directive = new AncoraDirective();
    expect(directive).toBeTruthy();
  });
});
